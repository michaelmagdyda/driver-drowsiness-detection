"""Unit tests - isolated functions and classes, no I/O.

Testing Strategy §5 targets ≥90% coverage for business logic at this level.
Nothing here touches the network, the database or the filesystem; anything that
needs a boundary crossed belongs in ``tests/integration``.
"""
